#include <windows.h>

int APIENTRY WinMain(
    HINSTANCE hInstance,
    HINSTANCE hPrevInstance,
    LPSTR     lpCmdLine,
    int       nCmdShow)
{
    // Your code here
    MessageBox(NULL, "Hello, World!", "Greetings", MB_OK);

    return 0;
}
